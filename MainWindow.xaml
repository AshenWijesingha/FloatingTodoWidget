<Window x:Class="FloatingTodoWidget.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:h="clr-namespace:FloatingTodoWidget.Helpers"
        xmlns:m="clr-namespace:FloatingTodoWidget.Models"
        Title="Tasks"
        Width="320" Height="480"
        MinWidth="240" MinHeight="220"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="True"
        Topmost="{Binding IsTopmost}">

    <Window.Resources>
        <h:PriorityToBrushConverter         x:Key="PriorityToBrush"/>
        <h:NullToVisibilityConverter        x:Key="NullToVis"/>
        <h:DueDateConverter                 x:Key="DueDate"/>
        <h:EmptyStringToVisibilityConverter x:Key="EmptyToVis"/>
        <BooleanToVisibilityConverter       x:Key="BoolToVis"/>
    </Window.Resources>

    <!-- Keyboard shortcuts -->
    <Window.InputBindings>
        <KeyBinding Modifiers="Control" Key="N" Command="{Binding FocusInputCommand}"/>
        <KeyBinding Modifiers="Control" Key="F" Command="{Binding FocusInputCommand}"/>
    </Window.InputBindings>

    <!-- Root surface: rounded, semi-transparent; sits over the acrylic blur. Drag from here. -->
    <Border x:Name="RootBorder"
            CornerRadius="14"
            Background="{DynamicResource WindowBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            MouseLeftButtonDown="RootBorder_MouseLeftButtonDown">

        <!-- Right-click menu -->
        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem Header="New task (Ctrl+N)" Command="{Binding FocusInputCommand}"/>
                <Separator/>
                <MenuItem Header="Show completed" IsCheckable="True" IsChecked="{Binding ShowCompleted}"/>
                <MenuItem Header="Dark theme"     IsCheckable="True" IsChecked="{Binding IsDarkTheme}"/>
                <MenuItem Header="Always on top"  IsCheckable="True" IsChecked="{Binding IsTopmost}"/>
                <MenuItem Header="Start with Windows" IsCheckable="True" IsChecked="{Binding AutoStart}"/>
                <Separator/>
                <MenuItem Header="Clear completed" Command="{Binding ClearCompletedCommand}"/>
                <Separator/>
                <MenuItem Header="Exit" Command="{Binding ExitCommand}"/>
            </ContextMenu>
        </Border.ContextMenu>

        <Grid Margin="14,12,14,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ===== Header ===== -->
            <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,10">
                <TextBlock DockPanel.Dock="Left" Text="Tasks" FontSize="16" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>

                <!-- pending count badge -->
                <Border DockPanel.Dock="Left" Margin="8,0,0,0" Padding="7,2"
                        CornerRadius="9" Background="{DynamicResource SurfaceBrush}"
                        VerticalAlignment="Center">
                    <TextBlock Text="{Binding PendingCount}" FontSize="11"
                               Foreground="{DynamicResource SubtleTextBrush}"/>
                </Border>

                <!-- close -->
                <Button DockPanel.Dock="Right" Style="{StaticResource IconButton}"
                        Command="{Binding ExitCommand}" ToolTip="Exit">
                    <Path Data="M0,0 L10,10 M10,0 L0,10" Stroke="{DynamicResource TextBrush}" StrokeThickness="1.6"/>
                </Button>

                <!-- theme toggle -->
                <Button DockPanel.Dock="Right" Style="{StaticResource IconButton}"
                        Click="ThemeToggle_Click" ToolTip="Toggle theme">
                    <Path Data="M5,0 A5,5 0 1 0 5,10 A3.2,3.2 0 1 1 5,0 Z"
                          Fill="{DynamicResource TextBrush}"/>
                </Button>
            </DockPanel>

            <!-- ===== Quick-add input ===== -->
            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- TextBox + placeholder overlay in the same cell -->
                <Grid Grid.Column="0">
                    <TextBox x:Name="InputBox"
                             Style="{StaticResource InputBox}"
                             Text="{Binding NewTaskText, UpdateSourceTrigger=PropertyChanged}"
                             PreviewKeyDown="InputBox_PreviewKeyDown"/>
                    <!-- Placeholder shown only when the input is empty -->
                    <TextBlock IsHitTestVisible="False"
                               Text="Quick add: 'Buy milk !high @2026-07-05'"
                               FontSize="13" Margin="10,0,0,0"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource SubtleTextBrush}"
                               Visibility="{Binding NewTaskText, Converter={StaticResource EmptyToVis}}"/>
                </Grid>

                <Button Grid.Column="1" Margin="8,0,0,0"
                        Style="{StaticResource AccentButton}"
                        Command="{Binding AddTaskCommand}" ToolTip="Add (Enter)">
                    <Path Data="M6,0 L6,12 M0,6 L12,6" Stroke="White" StrokeThickness="2"/>
                </Button>
            </Grid>

            <!-- ===== Task list ===== -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding TasksView}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type m:TodoItem}">
                            <!-- Fade + slide-in on load -->
                            <Border x:Name="row" Margin="0,2" Padding="6,7" CornerRadius="8"
                                    Background="Transparent" RenderTransformOrigin="0,0.5">
                                <Border.RenderTransform>
                                    <TranslateTransform x:Name="t" Y="8"/>
                                </Border.RenderTransform>
                                <Border.Triggers>
                                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                 From="0" To="1" Duration="0:0:0.18"/>
                                                <DoubleAnimation Storyboard.TargetName="t"
                                                                 Storyboard.TargetProperty="Y"
                                                                 From="8" To="0" Duration="0:0:0.18"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Border.Triggers>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="4"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Priority bar (click to cycle) -->
                                    <Border Grid.Column="0" Width="4" CornerRadius="2" Margin="0,1,8,1"
                                            Cursor="Hand"
                                            Background="{Binding Priority, Converter={StaticResource PriorityToBrush}}"
                                            MouseLeftButtonDown="Priority_Click"
                                            ToolTip="Click to change priority"/>

                                    <CheckBox Grid.Column="1" Margin="0,0,8,0"
                                              Style="{StaticResource FlatCheckBox}"
                                              IsChecked="{Binding IsCompleted}"/>

                                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                        <TextBlock x:Name="title" Text="{Binding Title}"
                                                   TextWrapping="Wrap" FontSize="13"
                                                   Foreground="{DynamicResource TextBrush}"/>
                                        <TextBlock Text="{Binding DueDate, Converter={StaticResource DueDate}}"
                                                   Visibility="{Binding DueDate, Converter={StaticResource NullToVis}}"
                                                   FontSize="10" Margin="0,2,0,0"
                                                   Foreground="{DynamicResource SubtleTextBrush}"/>
                                    </StackPanel>

                                    <Button Grid.Column="3" Style="{StaticResource IconButton}"
                                            Width="24" Height="24"
                                            Command="{Binding DataContext.DeleteTaskCommand,
                                                      RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}" ToolTip="Delete">
                                        <Path Data="M1,2 L9,2 M2,2 L2,9 L8,9 L8,2 M4,0 L6,0"
                                              Stroke="{DynamicResource SubtleTextBrush}" StrokeThickness="1.3"/>
                                    </Button>
                                </Grid>
                            </Border>

                            <DataTemplate.Triggers>
                                <!-- Completed look: strike-through + dim -->
                                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                    <Setter TargetName="title" Property="TextDecorations" Value="Strikethrough"/>
                                    <Setter TargetName="title" Property="Foreground" Value="{DynamicResource SubtleTextBrush}"/>
                                    <Setter TargetName="row" Property="Opacity" Value="0.6"/>
                                </DataTrigger>
                                <Trigger SourceName="row" Property="IsMouseOver" Value="True">
                                    <Setter TargetName="row" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                </Trigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- ===== Footer ===== -->
            <DockPanel Grid.Row="3" Margin="0,8,0,0" LastChildFill="False">
                <TextBlock DockPanel.Dock="Left" FontSize="11"
                           Foreground="{DynamicResource SubtleTextBrush}">
                    <Run Text="{Binding PendingCount, Mode=OneWay}"/><Run Text=" pending"/>
                </TextBlock>
                <Button DockPanel.Dock="Right" Style="{StaticResource IconButton}"
                        Width="Auto" Padding="8,2"
                        Command="{Binding ClearCompletedCommand}" ToolTip="Clear completed"
                        Visibility="{Binding HasCompleted, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="Clear done" FontSize="11"
                               Foreground="{DynamicResource SubtleTextBrush}"/>
                </Button>
            </DockPanel>
        </Grid>
    </Border>
</Window>
